import { StickerSettings } from "@/components";

import styles from "./style.module.scss";

export function App() {
  return (
    <div className={styles.app}>
      <StickerSettings />
    </div>
  );
}
