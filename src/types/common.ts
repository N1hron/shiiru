export type Dimensions = {
  width: number;
  height: number;
};

export type Segment = {
  start: number;
  end: number;
};

export type Rect = {
  top: number;
  left: number;
  width: number;
  height: number;
};

export type WidthHeight = Dimensions;
